apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quick-start-app.fullname" . }}-test-connection"
  labels:
    {{- include "quick-start-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quick-start-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
