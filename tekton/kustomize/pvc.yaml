apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-build-test
  labels:
    demo-devops/app: build
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce